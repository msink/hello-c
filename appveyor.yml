version: 1.0.{build}-{branch}

os:
  - Visual Studio 2015

platform:
  - x64

build:
  verbosity: detailed

environment:
  matrix:
    - compiler: msvc
      bit: 32
    - compiler: msvc
      bit: 64
    - compiler: mingw
      bit: 32
    - compiler: mingw
      bit: 64

# scripts that run after cloning repository
install:
  ############################################################################
  # Install Meson
  ############################################################################
  - set PYTHON_HOME=C:\Python35-x64
  - dir %PYTHON_HOME%
  - set PATH=%PYTHON_HOME%;%PATH%
  - python --version
  - pip3 install meson
  - meson --version

  ############################################################################
  # Install Ninja
  ############################################################################
  - ps: Start-FileDownload 'https://dl.dropboxusercontent.com/u/37517477/ninja.exe'
  - ninja --version

# to run your custom scripts instead of automatic MSBuild
build_script:
  - '%APPVEYOR_BUILD_FOLDER%\appveyor.bat'

# to disable automatic tests
test: off

# to disable deployment
deploy: off
